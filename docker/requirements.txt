# Core dependencies for the bookmarks server
# The server itself uses only Python standard library modules
# This file is kept for potential future dependencies

# No external dependencies required currently
# The server uses only: os, json, logging, subprocess, http.server 